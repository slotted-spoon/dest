<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slottedspoon.indoornav">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnterDestActivity"
            android:label="Dest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavigationActivity"
            android:label="Navigation"/>
        <activity
            android:name=".RouteOptionsActivity"
            android:label="@string/title_activity_route_options" />
        <activity android:name=".ArrivedActivity"
            android:label="Arrived"></activity>
    </application>

</manifest>